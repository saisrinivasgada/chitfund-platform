// Backward-compatible route for old deep links. The product now has one
// unified Tickets system rather than a separate Helpdesk.
export { default } from '../../../components/hub/HubTicketsScreen';
